USE oem_mini;

-- =========================
-- 1️⃣ Kiểm tra toàn bộ bảng users
-- =========================
SELECT * FROM users;

-- =========================
-- 2️⃣ Kiểm tra toàn bộ bảng courses
-- =========================
SELECT * FROM courses;

-- =========================
-- 3️⃣ Kiểm tra toàn bộ bảng exams
-- =========================
SELECT * FROM exams;

-- =========================
-- 4️⃣ Kiểm tra toàn bộ bảng exam_questions
-- =========================
SELECT * FROM exam_questions;

-- =========================
-- 5️⃣ Kiểm tra toàn bộ bảng exam_options
-- =========================
SELECT * FROM exam_options;

-- =========================
-- 6️⃣ Kiểm tra toàn bộ bảng student_answers
-- =========================
SELECT * FROM student_answers;

-- =========================
-- 7️⃣ Kiểm tra toàn bộ bảng submissions
-- =========================
SELECT * FROM submissions;

-- =========================
-- 8️⃣ Kiểm tra toàn bộ bảng results
-- =========================
SELECT * FROM results;

-- =========================
-- 9️⃣ Kiểm tra toàn bộ bảng ai_logs
-- =========================
SELECT * FROM ai_logs;

-- =========================
-- 🔟 Kiểm tra view tổng quan (v_exam_overview)
-- =========================
SELECT * FROM v_exam_overview;
